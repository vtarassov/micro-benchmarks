[project]
name = "documentdb-micro-benchmarks"
version = "0.1.0"
description = "Performance testing framework for DocumentDB, built on Locust"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "locust>=2.0.0",
    "numpy>=1.20.0",
    "posix-ipc>=1.0.0",
    "pymongo>=4.0.0",
]
